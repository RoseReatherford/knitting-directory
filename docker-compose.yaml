version: '1.0'

services:

  mongo:
    build: mongo/.
    restart: always
    # env_file: mongo/.env
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    ports:
      - "27017:27017"
    volumes:
      - ./mongo/db:/data/db
